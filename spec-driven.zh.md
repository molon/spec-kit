# 规格驱动开发（SDD）

## 权力反转

几十年来，代码一直是王道。规格说明为代码服务——它们是我们构建然后在"真正的工作"（编码）开始后丢弃的脚手架。我们编写 PRD 来指导开发，创建设计文档来通知实现，绘制图表来可视化架构。但这些总是从属于代码本身。代码是真理。其他一切最多是良好的意图。代码是真实来源，随着它向前发展，规格说明很少跟上。由于资产（代码）和实现是一体的，不容易有平行实现而不尝试从代码构建。

规格驱动开发（SDD）反转了这种权力结构。规格说明不为代码服务——代码为规格说明服务。产品需求文档（PRD）不是实现的指南；它是生成实现的来源。技术计划不是告知编码的文档；它们是产生代码的精确定义。这不是对我们构建软件方式的增量改进。这是对推动开发的东西的根本重新思考。

规格说明和实现之间的差距自软件开发诞生以来就困扰着它。我们尝试用更好的文档、更详细的需求、更严格的流程来弥合它。这些方法失败了，因为它们接受差距是不可避免的。他们尝试缩小它，但永远不会消除它。SDD 通过使规格说明和其具体实现计划可执行来消除差距。当规格说明和实现计划生成代码时，就没有差距——只有转换。

这种转换现在是可能的，因为 AI 可以理解和实现复杂的规格说明，并创建详细的实现计划。但没有结构的原始 AI 生成会产生混乱。SDD 通过足够精确、完整和明确的规格说明和后续实现计划来提供结构，以生成工作系统。规格说明成为主要工件。代码成为其在特定语言和框架中的表达（作为实现计划的实现）。

在这个新世界中，维护软件意味着演变规格说明。开发团队的意图用自然语言表达（"**意图驱动开发**"）、设计资产、核心原则和其他指导。开发的**通用语言**转向更高的级别，代码是最后一英里的方法。

调试意味着修复生成不正确代码的规格说明和实现计划。重构意味着为清晰而重新构建。整个开发工作流围绕规格说明作为中央真实来源重新组织，实现计划和代码作为持续再生的输出。使用新功能更新应用程序或创建新的并行实现，因为我们是创意的存在，意味着重新审视规格说明并创建新的实现计划。这个过程因此是 0 -> 1、(1'、..)、2、3、N。

开发团队专注于他们的创意、实验、他们的批判性思维。

## 实践中的 SDD 工作流

工作流从一个想法开始——通常是模糊和不完整的。通过与 AI 的迭代对话，这个想法变成了一个全面的 PRD。AI 提出澄清问题，识别边界情况，并帮助定义精确的验收标准。在传统开发中可能需要数天的会议和文档在专注规格说明工作的几小时内发生。这转变了传统 SDLC——需求和设计变成持续活动而不是离散阶段。这支持**团队流程**，其中团队审查的规格说明被表达和版本化，在分支中创建，并合并。

当产品经理更新验收标准时，实现计划自动标记受影响的技术决策。当架构师发现更好的模式时，PRD 更新以反映新的可能性。

在整个规格说明流程中，研究代理收集关键上下文。他们调查库兼容性、性能基准和安全含义。组织约束被发现并自动应用——你公司的数据库标准、身份验证要求和部署政策无缝集成到每个规格说明中。

从 PRD，AI 生成将需求映射到技术决策的实现计划。每个技术选择都有记录的理由。每个架构决策都追溯到特定的需求。在整个过程中，一致性验证持续改进质量。AI 分析规格说明的歧义、矛盾和差距——不是作为一次性门控，而是作为持续的细化。

一旦规格说明和其实现计划足够稳定，代码生成就开始了，但它们不必是"完整的"。早期生成可能是探索性的——测试规格说明在实践中是否有意义。域概念变成数据模型。用户故事变成 API 端点。验收场景变成测试。这通过规格说明合并了开发和测试——测试场景不是在代码之后编写的，它们是生成实现和测试的规格说明的一部分。

反馈循环超越了初始开发。生产指标和事件不仅仅触发热修复——它们更新规格说明以进行下一次再生。性能瓶颈变成新的非功能需求。安全漏洞变成影响所有未来生成的约束。规格说明、实现和操作现实之间的这种迭代舞蹈是真正理解出现的地方，以及传统 SDLC 转变为持续演变的地方。

## 为什么 SDD 现在很重要

三个趋势使 SDD 不仅可能而且必要：

首先，AI 能力已达到自然语言规格说明可以可靠地生成工作代码的阈值。这不是关于替换开发人员——这是关于通过自动化从规格说明到实现的机械翻译来放大他们的有效性。它可以放大探索和创意，支持"重新开始"轻松，并支持添加、减法和批判性思维。

其次，软件复杂性继续呈指数增长。现代系统集成了数十个服务、框架和依赖项。通过手动流程将所有这些部分与原始意图保持一致变得越来越困难。SDD 通过规格说明驱动的生成提供系统对齐。框架可能会发展为提供 AI 优先支持，而不是人类优先支持，或围绕可重用组件进行架构。

第三，变化的步伐加快。今天的需求变化远快于以往。转向不再是例外——这是预期的。现代产品开发要求基于用户反馈、市场条件和竞争压力进行快速迭代。传统开发将这些变化视为中断。每个转向都需要手动在文档、设计和代码中传播变化。结果要么是限制速度的缓慢、谨慎的更新，要么是积累技术债务的快速、鲁莽的更改。

SDD 可以支持假设/模拟实验："如果我们需要重新实现或改变应用程序以促进商业需求来销售更多 T 恤，我们将如何实现和实验？"

SDD 将需求变更从障碍转变为正常工作流。当规格说明驱动实现时，转向变成系统再生而不是手动重写。改变 PRD 中的核心需求，受影响的实现计划会自动更新。修改用户故事，相应的 API 端点会再生。这不仅仅是关于初始开发——这是关于通过不可避免的变化维持工程速度。

## 核心原则

**规格说明作为通用语言**：规格说明成为主要工件。代码成为其在特定语言和框架中的表达。维护软件意味着演变规格说明。

**可执行规格说明**：规格说明必须足够精确、完整和明确以生成工作系统。这消除了意图和实现之间的差距。

**持续细化**：一致性验证不是一次性门控。AI 作为持续流程分析规格说明的歧义、矛盾和差距。

**研究驱动的上下文**：研究代理在规格说明流程中收集关键上下文，调查技术选项、性能含义和组织约束。

**双向反馈**：生产现实告知规格说明演变。指标、事件和操作学习变成规格说明细化的输入。

**分支以进行探索**：从同一规格说明生成多个实现方法以探索不同的优化目标——性能、可维护性、用户体验、成本。

## 实现方法

今天，实践 SDD 需要组装现有工具并在整个流程中保持纪律。该方法论可以通过以下方式实践：

- AI 助手用于迭代规格说明开发
- 研究代理用于收集技术上下文
- 代码生成工具用于将规格说明转换为实现
- 为规格说明优先工作流调整的版本控制系统
- 通过 AI 分析规格说明文档的一致性检查

关键是将规格说明视为真实来源，代码作为为规格说明服务的生成输出，而不是相反。

## 使用命令简化 SDD

SDD 方法论通过三个强大的命令得到显著增强，这些命令自动化规格说明 → 规划 → 任务工作流：

### `/speckit.specify` 命令

此命令将简单的功能描述（用户提示）转换为完整的结构化规格说明，具有自动仓库管理：

1. **自动功能编号**：扫描现有规格说明以确定下一个功能编号（例如 001、002、003）
2. **分支创建**：从你的描述生成语义分支名称并自动创建它
3. **基于模板的生成**：复制并自定义功能规格说明模板与你的需求
4. **目录结构**：为所有相关文档创建适当的 `specs/[branch-name]/` 结构

### `/speckit.plan` 命令

一旦功能规格说明存在，此命令创建全面的实现计划：

1. **规格说明分析**：读取并理解功能需求、用户故事和验收标准
2. **宪法合规**：确保与项目宪法和架构原则的对齐
3. **技术翻译**：将业务需求转换为技术架构和实现细节
4. **详细文档**：为数据模型、API 合约和测试场景生成支持文档
5. **快速启动验证**：产生捕获关键验证场景的快速启动指南

### `/speckit.tasks` 命令

创建计划后，此命令分析计划和相关设计文档以生成可执行的任务列表：

1. **输入**：读取 `plan.md`（必需）以及如果存在，`data-model.md`、`contracts/` 和 `research.md`
2. **任务推导**：将合约、实体和场景转换为特定任务
3. **并行化**：标记独立任务 `[P]` 并概述安全的并行组
4. **输出**：在功能目录中写入 `tasks.md`，准备由任务代理执行

### 示例：构建聊天功能

以下是这些命令如何转变传统开发工作流：

**传统方法：**

```text
1. 在文档中编写 PRD（2-3 小时）
2. 创建设计文档（2-3 小时）
3. 手动设置项目结构（30 分钟）
4. 编写技术规格说明（3-4 小时）
5. 创建测试计划（2 小时）
总计：~12 小时的文档工作
```

**带命令的 SDD 方法：**

```bash
# 步骤 1：创建功能规格说明（5 分钟）
/speckit.specify 具有消息历史和用户存在的实时聊天系统

# 这自动：
# - 创建分支"003-chat-system"
# - 生成 specs/003-chat-system/spec.md
# - 用结构化需求填充它

# 步骤 2：生成实现计划（5 分钟）
/speckit.plan WebSocket 用于实时消息传递，PostgreSQL 用于历史，Redis 用于存在

# 步骤 3：生成可执行任务（5 分钟）
/speckit.tasks

# 这自动创建：
# - specs/003-chat-system/plan.md
# - specs/003-chat-system/research.md（WebSocket 库比较）
# - specs/003-chat-system/data-model.md（消息和用户模式）
# - specs/003-chat-system/contracts/（WebSocket 事件、REST 端点）
# - specs/003-chat-system/quickstart.md（关键验证场景）
# - specs/003-chat-system/tasks.md（从计划推导的任务列表）
```

在 15 分钟内，你有：

- 具有用户故事和验收标准的完整功能规格说明
- 具有技术选择和理由的详细实现计划
- 为代码生成准备好的 API 合约和数据模型
- 用于自动化和手动测试的全面测试场景
- 在功能分支中正确版本化的所有文档

### 结构化自动化的力量

这些命令不仅节省时间——它们强制一致性和完整性：

1. **没有遗忘的细节**：模板确保考虑每个方面，从非功能需求到错误处理
2. **可追踪的决策**：每个技术选择都链接回特定的需求
3. **生活文档**：规格说明保持与代码同步，因为它们生成它
4. **快速迭代**：在几分钟内改变需求和再生计划，而不是几天

这些命令体现了 SDD 原则，通过将规格说明视为可执行工件而不是静态文档，将规格说明流程从必要的邪恶转变为开发的驱动力。

### 模板驱动的质量：结构如何约束 LLM 以获得更好的结果

这些命令的真正力量不仅在于自动化，还在于模板如何指导 LLM 行为朝向更高质量的规格说明。模板充当复杂的提示，以生产方式约束 LLM 的输出：

#### 1. **防止过早的实现细节**

功能规格说明模板明确指示：

```text
- ✅ 专注于用户需要什么以及为什么
- ❌ 避免如何实现（无技术栈、API、代码结构）
```

此约束强制 LLM 保持适当的抽象级别。当 LLM 可能自然地跳转到"使用 React with Redux 实现"时，模板将其保持在"用户需要他们数据的实时更新"上。此分离确保规格说明即使在实现技术改变时也保持稳定。

#### 2. **强制显式不确定性标记**

两个模板都强制使用 `[NEEDS CLARIFICATION]` 标记：

```text
创建此规格说明时：
1. **标记所有歧义**：使用 [NEEDS CLARIFICATION: specific question]
2. **不要猜测**：如果提示没有指定某些内容，标记它
```

这防止了常见的 LLM 行为，即做出看似合理但可能不正确的假设。而不是猜测"登录系统"使用电子邮件/密码身份验证，LLM 必须标记它为 `[NEEDS CLARIFICATION: auth method not specified - email/password, SSO, OAuth?]`。

#### 3. **通过检查清单进行结构化思维**

模板包括充当规格说明"单元测试"的全面检查清单：

```markdown
### 需求完整性

- [ ] 没有 [NEEDS CLARIFICATION] 标记保留
- [ ] 需求是可测试和明确的
- [ ] 成功标准是可测量的
```

这些检查清单强制 LLM 系统地自我审查其输出，捕捉可能滑过的差距。这就像给 LLM 一个质量保证框架。

#### 4. **通过门控进行宪法合规**

实现计划模板通过阶段门控强制架构原则：

```markdown
### 阶段 -1：前实现门控

#### 简单性门控（第七条）

- [ ] 使用 ≤3 个项目？
- [ ] 没有未来证明？

#### 反抽象门控（第八条）

- [ ] 直接使用框架？
- [ ] 单一模型表示？
```

这些门控通过使 LLM 明确证明任何复杂性来防止过度工程。如果门控失败，LLM 必须在"复杂性跟踪"部分中记录原因，为架构决策创建问责。

#### 5. **分层细节管理**

模板强制适当的信息架构：

```text
**重要**：此实现计划应保持高级和可读。
任何代码示例、详细算法或广泛的技术规格说明
必须放在适当的 `implementation-details/` 文件中
```

这防止了规格说明变成不可读的代码转储的常见问题。LLM 学会保持适当的详细级别，将复杂性提取到单独的文件，同时保持主文档可导航。

#### 6. **测试优先思维**

实现模板强制测试优先开发：

```text
### 文件创建顺序
1. 使用 API 规格说明创建 `contracts/`
2. 按顺序创建测试文件：合约 → 集成 → e2e → 单元
3. 创建源文件以使测试通过
```

此排序约束确保 LLM 在实现之前考虑可测试性和合约，导致更强大和可验证的规格说明。

#### 7. **防止投机功能**

模板明确劝阻投机：

```text
- [ ] 没有投机或"可能需要"功能
- [ ] 所有阶段都有明确的前置条件和可交付成果
```

这阻止 LLM 添加使实现复杂化的"很好拥有"功能。每个功能必须追溯到具有明确验收标准的具体用户故事。

### 复合效应

这些约束一起产生规格说明，这些规格说明是：

- **完整**：检查清单确保没有遗忘任何内容
- **明确**：强制澄清标记突出不确定性
- **可测试**：测试优先思维烘焙到流程中
- **可维护**：适当的抽象级别和信息层级
- **可实现**：具有具体可交付成果的清晰阶段

模板将 LLM 从创意作家转变为纪律规格说明工程师，将其能力引导朝向一致地生成高质量、可执行的规格说明，真正驱动开发。

## 宪法基础：强制架构纪律

在 SDD 的核心是一部宪法——一套不可改变的原则，管理规格说明如何变成代码。宪法（`memory/constitution.md`）充当系统的架构 DNA，确保每个生成的实现维持一致性、简单性和质量。

### 开发的九条文章

宪法定义了九条文章，这些文章塑造了开发流程的每个方面：

#### 第一条：库优先原则

每个功能必须开始为独立库——没有例外。这从一开始就强制模块化设计：

```text
Specify 中的每个功能必须开始其存在为独立库。
没有功能应在没有首先抽象为可重用库组件的应用程序代码中直接实现。
```

此原则确保规格说明生成模块化、可重用的代码，而不是单一应用程序。当 LLM 生成实现计划时，它必须将功能构建为具有清晰边界和最小依赖项的库。

#### 第二条：CLI 接口强制

每个库必须通过命令行界面公开其功能：

```text
所有 CLI 接口必须：
- 接受文本作为输入（通过 stdin、参数或文件）
- 通过 stdout 生成文本作为输出
- 支持结构化数据交换的 JSON 格式
```

这强制可观测性和可测试性。LLM 不能将功能隐藏在不透明的类中——一切都必须通过基于文本的接口可访问和可验证。

#### 第三条：测试优先命令

最具转变性的文章——代码前没有测试：

```text
这是不可协商的：所有实现必须遵循严格的测试驱动开发。
在以下之前不应编写实现代码：
1. 单元测试被编写
2. 测试由用户验证和批准
3. 测试被确认为失败（红色阶段）
```

这完全反转了传统的 AI 代码生成。而不是生成代码并希望它有效，LLM 必须首先生成定义行为的全面测试，获得批准，然后才生成实现。

#### 第七和第八条：简单性和反抽象

这些配对的文章对抗过度工程：

```text
第 7.3 节：最小项目结构
- 初始实现的最多 3 个项目
- 额外项目需要记录的理由

第 8.1 节：框架信任
- 直接使用框架功能而不是包装它们
```

当 LLM 可能自然地创建精心设计的抽象时，这些文章强制它证明每一层复杂性。实现计划模板的"阶段 -1 门控"直接强制这些原则。

#### 第九条：集成优先测试

优先考虑真实世界测试而不是隔离单元测试：

```text
测试必须使用现实环境：
- 更喜欢真实数据库而不是模拟
- 使用实际服务实例而不是存根
- 合约测试在实现前强制
```

这确保生成的代码在实践中有效，而不仅仅是理论上。

### 通过模板的宪法执行

实现计划模板通过具体检查点操作化这些文章：

```markdown
### 阶段 -1：前实现门控

#### 简单性门控（第七条）

- [ ] 使用 ≤3 个项目？
- [ ] 没有未来证明？

#### 反抽象门控（第八条）

- [ ] 直接使用框架？
- [ ] 单一模型表示？

#### 集成优先门控（第九条）

- [ ] 定义了合约？
- [ ] 编写了合约测试？
```

这些门控充当架构原则的编译时检查。LLM 不能在没有通过门控或在"复杂性跟踪"部分中记录合理的例外的情况下继续。

### 不可改变原则的力量

宪法的力量在于其不可改变性。虽然实现细节可以演变，核心原则保持不变。这提供：

1. **跨时间的一致性**：今天生成的代码遵循与明年生成的代码相同的原则
2. **跨 LLM 的一致性**：不同的 AI 模型生成架构兼容的代码
3. **架构完整性**：每个功能强化而不是破坏系统设计
4. **质量保证**：测试优先、库优先和简单性原则确保可维护的代码

### 宪法演变

虽然原则是不可改变的，但它们的应用可以演变：

```text
第 4.2 节：修订流程
对此宪法的修改需要：
- 变更理由的明确文档
- 项目维护者的审查和批准
- 向后兼容性评估
```

这允许方法论学习和改进，同时保持稳定性。宪法通过日期修订展示了自己的演变，演示了原则如何可以根据真实世界经验进行细化。

### 超越规则：发展哲学

宪法不仅仅是一本规则书——它是一种塑造 LLM 如何思考代码生成的哲学：

- **可观测性优于不透明**：一切都必须通过 CLI 接口可检查
- **简单性优于聪慧**：开始简单，仅在证明必要时添加复杂性
- **集成优于隔离**：在真实环境中测试，而不是人工环境
- **模块化优于单一**：每个功能都是具有清晰边界的库

通过将这些原则嵌入规格说明和规划流程，SDD 确保生成的代码不仅仅是功能性的——它是可维护的、可测试的和架构上合理的。宪法将 AI 从代码生成器转变为架构合作伙伴，尊重和强化系统设计原则。

## 转变

这不是关于替换开发人员或自动化创意。这是关于通过自动化机械翻译来放大人类能力。这是关于创建一个紧密的反馈循环，其中规格说明、研究和代码一起演变，每次迭代带来更深的理解和更好的意图和实现之间的对齐。

软件开发需要更好的工具来维持意图和实现之间的对齐。SDD 通过生成代码而不仅仅指导代码的可执行规格说明提供了实现此对齐的方法论。
